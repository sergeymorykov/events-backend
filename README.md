# events-backend

Бэкенд часть приложения для поиска мероприятий из Telegram-каналов.

> 🚀 **[Быстрый старт →](QUICKSTART.md)** — начните работу за 5 минут!

## Telegram Parser v2.0

Продвинутый асинхронный парсер для извлечения постов о мероприятиях из публичных Telegram-каналов с умной фильтрацией и сохранением в MongoDB.

### ✨ Возможности

- ✅ **Асинхронная работа** — использует Telethon для быстрого парсинга
- ✅ **Автоматический планировщик** — запуск парсинга по расписанию (каждые 24 часа)
- ✅ **Парсинг по периоду** — собирает посты за последние N месяцев (вместо лимита)
- ✅ **Множественные каналы** — парсинг нескольких каналов одновременно
- ✅ **Приватные каналы** — поддержка invite links (+hash формат)
- ✅ **Умная фильтрация**:
  - Фильтрация по whitelist/blacklist хештегов
  - Автоматический парсинг дат из текста (множество форматов)
  - Игнорирование прошедших мероприятий
- ✅ **Парсинг дат** — поддержка форматов:
  - Текстовые: "23 ноября", "23 ноября 2025"
  - Числовые: "10.12.2025", "2025-12-10"
  - Относительные: "сегодня", "завтра", "послезавтра"
- ✅ **Извлечение хештегов** — регистронезависимое извлечение всех хештегов
- ✅ **MongoDB** — сохранение в коллекцию `raw_posts` с проверкой дубликатов
- ✅ **Сессии** — авторизация один раз, повторный ввод кода не требуется
- ✅ **Обработка ошибок** — автоматическая обработка rate limits и ошибок
- ✅ **Подробное логирование** — все операции логируются
- ✅ **Модульная архитектура** — чистый код, легко расширяемый

### 📁 Структура проекта

```
events-backend/
├── telegram_parser/           # Основной модуль парсера
│   ├── __init__.py           # Инициализация модуля
│   ├── config.py             # Конфигурация из .env
│   ├── parser.py             # Основная логика парсера
│   ├── filters.py            # Фильтрация по хештегам
│   ├── date_parser.py        # Парсинг дат из текста
│   ├── main.py               # Точка входа (можно запускать)
│   ├── check_config.py       # Проверка конфигурации
│   └── download_photos.py    # Скачивание фото (опционально)
├── run_parser.py             # Скрипт запуска из корня
├── requirements.txt          # Зависимости
├── env.example               # Пример конфигурации
├── .gitignore               # Игнорируемые файлы
├── README.md                # Этот файл
└── QUICKSTART.md            # Быстрый старт
```

### 🔧 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repo-url>
cd events-backend
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте файл `.env`:**
```bash
cp env.example .env
```

4. **Заполните `.env`:**

```env
# Получите credentials на https://my.telegram.org/apps
TG_API_ID=12345678
TG_API_HASH=0123456789abcdef0123456789abcdef

# Каналы (через запятую) - публичные и приватные
CHANNEL_USERNAME=durov,telegram,+lyO6V_0sqeA3OWRi

# Глобальные фильтры хештегов (опционально, применяются ко всем каналам)
HASHTAG_WHITELIST=событие,мероприятие
HASHTAG_BLACKLIST=спам,реклама

# Специфичные фильтры для отдельных каналов (опционально)
# Переопределяют глобальные фильтры для конкретного канала
CHANNEL_durov_WHITELIST=фестиваль,концерт,выставка
CHANNEL_durov_BLACKLIST=18+,закрытое

CHANNEL_telegram_WHITELIST=новости,обновления
CHANNEL_telegram_BLACKLIST=партнерство

# MongoDB
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=events_db

# Настройки
MONTHS_BACK=3  # Парсинг постов за последние 3 месяца
TG_SESSION_NAME=telegram_parser_session
```

### 🚀 Использование

#### 1. Проверка конфигурации

Рекомендуется перед первым запуском:
```bash
python telegram_parser/check_config.py
```

Скрипт проверит:
- ✅ Наличие всех переменных окружения
- ✅ Подключение к MongoDB
- ✅ Наличие сессии Telethon
- ✅ Настройки фильтров

#### 2. Режимы работы

**🔹 Разовый запуск**
Парсит все каналы один раз и завершается:
```bash
python run_parser.py
```

**🔹 Автоматический режим (рекомендуется)**
Запускает парсинг по расписанию (каждые 24 часа):
```bash
python run_scheduler.py
```

Преимущества автоматического режима:
- 🤖 Автоматически проверяет новые посты
- 🔄 Не дублирует уже сохраненные посты
- 📊 Непрерывный мониторинг каналов
- 💾 Отдельный лог-файл `telegram_scheduler.log`

#### 3. Авторизация в Telegram

При первом запуске (разовом или планировщике):
1. Введите номер телефона
2. Введите код из Telegram
3. Если 2FA — введите пароль

Сессия сохраняется в файле `*.session`, повторная авторизация не требуется.

#### 4. Скачивание фото (опционально)

```bash
python telegram_parser/download_photos.py
# или с лимитом
python telegram_parser/download_photos.py --limit 10
```

### 📊 Структура данных в MongoDB

Коллекция `raw_posts`:

```javascript
{
    // Основная информация
    "post_id": 123,                    // ID сообщения (unique index)
    "channel": "durov",                // Username канала
    "text": "Фестиваль 23 ноября...", // Текст поста
    
    // Дата мероприятия (распознанная из текста)
    "date_parsed": ISODate("2025-11-23T00:00:00Z"),
    
    // Хештеги (в нижнем регистре, без #)
    "hashtags": ["фестиваль", "музыка", "москва"],
    
    // URL и медиа
    "post_url": "https://t.me/durov/123",
    "photo_url": {                     // Если есть фото
        "photo_id": 123456789,
        "access_hash": 987654321,
        "width": 1280,
        "height": 720,
        ...
    },
    
    // Статистика
    "views": 1000,
    "forwards": 50,
    
    // Метаданные
    "message_date": ISODate("2025-11-20T12:00:00Z"),  // Дата публикации
    "parsed_at": ISODate("2025-11-20T13:00:00Z"),     // Время парсинга
    
    // Фильтрация (null если пост валиден)
    "filtered_reason": null,
    // или одно из:
    // - "no_hashtags_with_whitelist"
    // - "not_in_whitelist"
    // - "blacklist:спам,реклама"
    // - "no_date_found"
    // - "date_in_past"
}
```

### 🎯 Логика фильтрации

#### Фильтр по хештегам:

1. **Извлечение хештегов**: регулярным выражением `#[\w\d_]+` (регистронезависимо)
2. **Уровни фильтров**:
   - **Глобальные** (HASHTAG_WHITELIST / HASHTAG_BLACKLIST) — применяются ко всем каналам по умолчанию
   - **Специфичные** (CHANNEL_{channel}_WHITELIST / CHANNEL_{channel}_BLACKLIST) — переопределяют глобальные для конкретного канала
3. **Blacklist** (приоритет выше):
   - Если хотя бы один хештег из поста в blacklist → отфильтровать
4. **Whitelist**:
   - Если задан и ни один хештег не в whitelist → отфильтровать
   - Если не задан → все хештеги разрешены

#### Фильтр по датам:

1. **Парсинг даты** из текста (первое совпадение):
   - "23 ноября" → 23.11.{текущий_год}
   - "10.12.2025" → 10.12.2025
   - "сегодня" → сегодняшняя дата
   - и другие форматы
2. **Если дата не найдена** → отфильтровать (`no_date_found`)
3. **Если дата ≤ сегодня** → отфильтровать (`date_in_past`)
4. **Иначе** → сохранить

### 📝 Логирование

Логи сохраняются:
- **Файл**: `telegram_parser.log`
- **Консоль**: stdout

Уровни:
- `INFO` — основные операции, статистика
- `WARNING` — rate limits
- `ERROR` — ошибки обработки
- `CRITICAL` — критические ошибки

Пример вывода:
```
🚀 ЗАПУСК TELEGRAM ПАРСЕРА
Каналы: durov, telegram
Whitelist хештегов: фестиваль, выставка
Лимит постов на канал: 100
...
📊 СТАТИСТИКА ПАРСИНГА:
  Всего постов обработано: 200
  ✅ Сохранено валидных постов: 45
  ❌ Отфильтровано по хештегам: 30
  ❌ Отфильтровано по дате: 15
  ❌ Без даты в тексте: 100
  ⏭️  Дубликаты: 10
```

### 🛠️ Обработка ошибок

- **FloodWaitError** — автоматическое ожидание + повтор
- **ChannelPrivateError** — логирование ошибки, переход к следующему каналу
- **ChannelInvalidError** — некорректный username
- **DuplicateKeyError** — пост уже существует (пропускается)
- **ServerSelectionTimeoutError** — MongoDB недоступна

### 🧪 Примеры использования

#### Парсинг с глобальными фильтрами:
```env
CHANNEL_USERNAME=events1,events2,events3
HASHTAG_WHITELIST=фестиваль,концерт,выставка
HASHTAG_BLACKLIST=спам,реклама
# Все каналы используют одинаковые фильтры
```

#### Парсинг с индивидуальными фильтрами для каждого канала:
```env
CHANNEL_USERNAME=moscowevents,spbevents,kulturaru

# Для moscowevents - только музыкальные события
CHANNEL_moscowevents_WHITELIST=концерт,фестиваль,джаз,рок
CHANNEL_moscowevents_BLACKLIST=18+

# Для spbevents - культурные мероприятия
CHANNEL_spbevents_WHITELIST=выставка,театр,музей
CHANNEL_spbevents_BLACKLIST=реклама,партнерство

# Для kulturaru - используются глобальные фильтры (если заданы)
```

#### Комбинированный подход (глобальные + специфичные):
```env
CHANNEL_USERNAME=channel1,channel2,channel3

# Глобальные фильтры (по умолчанию для всех)
HASHTAG_WHITELIST=событие,мероприятие
HASHTAG_BLACKLIST=спам,реклама

# Переопределяем только для channel1
CHANNEL_channel1_WHITELIST=концерт,фестиваль
# channel1 использует глобальный blacklist

# channel2 и channel3 используют глобальные фильтры
```

#### Просмотр статистики в MongoDB:
```javascript
// Общее количество постов
db.raw_posts.count()

// Валидные посты (не отфильтрованные)
db.raw_posts.count({ filtered_reason: null })

// Отфильтрованные по датам
db.raw_posts.count({ filtered_reason: "date_in_past" })

// Посты с конкретным хештегом
db.raw_posts.find({ hashtags: "фестиваль" })

// Будущие мероприятия
db.raw_posts.find({ 
    filtered_reason: null,
    date_parsed: { $gte: new Date() }
}).sort({ date_parsed: 1 })
```

### 📦 Технический стек

- **Python 3.11+**
- **Telethon** — асинхронный Telegram API клиент
- **pymongo** — MongoDB драйвер
- **python-dotenv** — переменные окружения
- **asyncio** — асинхронность

### 🤝 Разработка

Модульная архитектура позволяет легко расширять функциональность:

- `config.py` — добавление новых параметров
- `filters.py` — новые фильтры
- `date_parser.py` — новые форматы дат
- `parser.py` — основная логика

### 🔐 Приватные каналы

Парсер поддерживает приватные каналы:

```env
# Рекомендуется: по ID канала
CHANNEL_USERNAME=durov,-1001234567890,telegram

# Альтернатива: по invite hash
CHANNEL_USERNAME=durov,+lyO6V_0sqeA3OWRi,telegram
```

**Получить ID канала:**
```bash
python get_channel_id.py
```

Подробнее: **[PRIVATE_CHANNELS.md](PRIVATE_CHANNELS.md)** | **[GET_CHANNEL_ID.md](GET_CHANNEL_ID.md)**

### 📄 Лицензия

См. [LICENSE](LICENSE)
