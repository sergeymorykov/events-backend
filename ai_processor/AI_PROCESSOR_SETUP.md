# AI Processor - Полная инструкция по настройке

## 📋 Что было создано

### Модуль AI Processor

```
ai_processor/
├── __init__.py           # Экспорты модуля
├── config.py             # Конфигурация из ENV
├── models.py             # Pydantic модели
├── processor.py          # Основной процессор
├── image_handler.py      # Работа с изображениями
├── llm_handler.py        # Интеграция с LLM
├── db_handler.py         # Работа с MongoDB
└── README.md             # Документация
```

### Скрипты запуска

- `run_ai_processor.py` - обработка всех постов
- `test_single_post.py` - тестирование на одном посте

### Документация

- `AI_PROCESSOR_QUICKSTART.md` - быстрый старт
- `ai_processor/README.md` - полная документация
- `AI_PROCESSOR_SETUP.md` - этот файл

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `pydantic==2.9.2` - валидация данных
- `aiohttp==3.10.10` - асинхронные HTTP запросы

### Шаг 2: Настройка переменных окружения

Добавьте в ваш `.env` файл:

```bash
# ===== AI PROCESSOR =====

# Провайдер LLM (gigachat или openai)
LLM_PROVIDER=gigachat

# GigaChat (если LLM_PROVIDER=gigachat)
GIGACHAT_TOKEN=ваш_токен_gigachat

# OpenAI (если LLM_PROVIDER=openai)
# OPENAI_API_KEY=ваш_ключ_openai
# OPENAI_BASE_URL=https://api.openai.com/v1

# Kandinsky для генерации изображений
KANDINSKY_API_KEY=ваш_ключ_kandinsky
KANDINSKY_SECRET_KEY=ваш_секретный_ключ

# Папка для изображений
IMAGES_DIR=images
```

### Шаг 3: Получение API ключей

#### GigaChat (рекомендуется для русского языка)

1. Перейдите: https://developers.sber.ru/portal/products/gigachat
2. Зарегистрируйтесь и создайте проект
3. Получите токен авторизации
4. Вставьте в `.env`: `GIGACHAT_TOKEN=ваш_токен`

#### OpenAI (альтернатива)

1. Перейдите: https://platform.openai.com/api-keys
2. Создайте API ключ
3. Вставьте в `.env`: `OPENAI_API_KEY=ваш_ключ`

#### Kandinsky 3.1 (генерация изображений)

1. Перейдите: https://fusionbrain.ai/
2. Зарегистрируйтесь
3. Получите API Key и Secret Key
4. Вставьте в `.env`

> ⚠️ **Примечание:** Kandinsky опционален. Без него модуль будет работать только с постами, в которых уже есть фото.

### Шаг 4: Запуск

#### Тестирование на одном посте

```bash
python test_single_post.py
```

Или с тестовыми данными:

```bash
python test_single_post.py --mock
```

#### Полная обработка

```bash
python run_ai_processor.py
```

## 🔄 Полный рабочий процесс

### 1. Парсинг постов из Telegram

```bash
# Разовый запуск
python run_parser.py

# Или автоматический режим (каждые 24 часа)
python run_scheduler.py
```

**Результат:** посты сохраняются в коллекцию `raw_posts`

### 2. Обработка через AI

```bash
python run_ai_processor.py
```

**Что происходит:**
1. Загрузка необработанных постов из `raw_posts`
2. Для каждого поста:
   - Обработка изображения (скачивание или генерация)
   - Генерация описания изображения (если есть)
   - Извлечение данных через LLM
   - Сохранение в `processed_events`
   - Обновление `categories` и `user_interests`

**Результат:** структурированные события в MongoDB

### 3. Использование данных

События доступны в коллекции `processed_events`:

```javascript
// Пример запроса
db.processed_events.find({
  date: { $gte: new Date() },
  categories: "концерт"
}).sort({ date: 1 })
```

## 📊 Структура данных

### Входные данные (raw_posts)

```json
{
  "post_id": 12345,
  "text": "Концерт 25 ноября в 19:00, билеты от 1500₽",
  "photo_url": {"photo_id": "...", "access_hash": "..."},
  "post_url": "https://t.me/channel/12345",
  "hashtags": ["концерт", "музыка"]
}
```

### Выходные данные (processed_events)

```json
{
  "title": "Концерт джазовой музыки",
  "description": "Вечер живой музыки с лучшими композициями",
  "date": "2025-11-25T19:00:00",
  "price": {
    "amount": 1500,
    "currency": "RUB"
  },
  "categories": ["концерт", "музыка"],
  "user_interests": ["джаз", "живая музыка"],
  "image_url": "images/generated_20251123_120000.png",
  "image_caption": "Изображение концертного зала с музыкантами",
  "source_post_url": "https://t.me/channel/12345",
  "raw_post_id": 12345,
  "processed_at": "2025-11-23T12:00:00"
}
```

### Категории (categories)

```json
{
  "name": "концерт",
  "usage_count": 42,
  "created_at": "2025-11-23T10:00:00"
}
```

### Интересы (user_interests)

```json
{
  "name": "живая музыка",
  "usage_count": 38,
  "created_at": "2025-11-23T10:00:00"
}
```

## 🛠️ Настройка и конфигурация

### Выбор LLM провайдера

#### GigaChat (рекомендуется)

**Преимущества:**
- Оптимизирован для русского языка
- Лучше понимает российский контекст
- Модель GigaChat-Max для сложных задач

**Настройка:**
```bash
LLM_PROVIDER=gigachat
GIGACHAT_TOKEN=ваш_токен
```

#### OpenAI

**Преимущества:**
- Высокое качество ответов
- Поддержка vision-моделей
- Стабильный API

**Настройка:**
```bash
LLM_PROVIDER=openai
OPENAI_API_KEY=ваш_ключ
```

### Kandinsky 3.1

**Когда используется:**
- Если у поста нет изображения
- Генерация на основе текста поста

**Настройка:**
```bash
KANDINSKY_API_KEY=ваш_ключ
KANDINSKY_SECRET_KEY=ваш_секрет
```

**Если не настроено:**
- Модуль будет работать
- Обрабатываются только посты с фото
- Посты без фото пропускаются

### Telegram API (опционально)

**Для чего:**
- Скачивание фото из Telegram постов

**Если не настроено:**
- Используются только URL фото
- Генерация через Kandinsky работает

**Настройка:**
```bash
TG_API_ID=ваш_api_id
TG_API_HASH=ваш_api_hash
```

## 📝 Логирование

### Файлы логов

- `ai_processor.log` - все операции AI Processor
- `telegram_parser.log` - парсинг Telegram

### Просмотр логов

```bash
# В реальном времени
tail -f ai_processor.log

# Последние 100 строк
tail -n 100 ai_processor.log

# Поиск ошибок
grep "ERROR" ai_processor.log
```

### Пример логов

```
2025-11-23 12:00:00 | INFO | Обработка поста ID: 12345
2025-11-23 12:00:05 | INFO | Генерация изображения через Kandinsky...
2025-11-23 12:00:20 | INFO | Изображение сгенерировано
2025-11-23 12:00:25 | INFO | Генерация данных через LLM...
2025-11-23 12:00:30 | INFO | ✅ Пост обработан успешно
```

## 🐛 Troubleshooting

### Проблема: "Нет необработанных постов в БД"

**Решение:**
```bash
python run_parser.py
```

Сначала нужно собрать посты из Telegram.

### Проблема: "GIGACHAT_TOKEN должен быть указан"

**Решение:**
1. Проверьте файл `.env`
2. Убедитесь, что `GIGACHAT_TOKEN` заполнен
3. Перезапустите скрипт

### Проблема: "Не удалось подключиться к MongoDB"

**Решение:**
```bash
# Проверьте, что MongoDB запущена
mongod

# Или используйте MongoDB Atlas
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/
```

### Проблема: "Kandinsky API ключи не настроены"

**Это предупреждение, не ошибка.**

Модуль будет работать, но:
- Генерация изображений недоступна
- Обрабатываются только посты с фото

**Решение:**
1. Получите ключи на https://fusionbrain.ai/
2. Добавьте в `.env`:
```bash
KANDINSKY_API_KEY=ваш_ключ
KANDINSKY_SECRET_KEY=ваш_секрет
```

### Проблема: "Таймаут при вызове LLM"

**Причины:**
- Медленный интернет
- Перегрузка API

**Решение:**
- Подождите и повторите
- Проверьте интернет-соединение
- Модуль вернет частичный результат

### Проблема: "Ошибка парсинга JSON от LLM"

**Причина:** LLM вернул невалидный JSON

**Что делает модуль:**
- Логирует ошибку
- Пытается извлечь JSON из markdown
- Возвращает частичный результат

**Проверка:**
1. Посмотрите логи: `grep "JSON" ai_processor.log`
2. Проверьте, какой промпт отправлялся

## 📚 Дополнительные ресурсы

### Документация

- [AI_PROCESSOR_QUICKSTART.md](AI_PROCESSOR_QUICKSTART.md) - быстрый старт
- [ai_processor/README.md](ai_processor/README.md) - полная документация
- [telegram_parser/README.md](telegram_parser/README.md) - документация парсера

### API документация

- [GigaChat API](https://developers.sber.ru/portal/products/gigachat/documentation)
- [OpenAI API](https://platform.openai.com/docs/api-reference)
- [Kandinsky API](https://fusionbrain.ai/docs)

### Примеры

См. файл `test_single_post.py` для примеров использования API.

## 💡 Полезные команды

```bash
# Полная обработка всех постов
python run_ai_processor.py

# Тест на одном посте из БД
python test_single_post.py

# Тест с mock данными
python test_single_post.py --mock

# Просмотр логов
tail -f ai_processor.log

# Статистика БД
mongo
> use events_db
> db.processed_events.count()
> db.categories.find().sort({usage_count: -1}).limit(10)
```

## 🎉 Готово!

Теперь у вас есть полноценный AI Processor для обработки постов из Telegram!

**Следующие шаги:**
1. Настройте API ключи в `.env`
2. Запустите тест: `python test_single_post.py --mock`
3. Соберите посты: `python run_parser.py`
4. Обработайте через AI: `python run_ai_processor.py`
5. Проверьте результаты в MongoDB

**Поддержка:**
- 📖 Полная документация: [ai_processor/README.md](ai_processor/README.md)
- 🚀 Быстрый старт: [AI_PROCESSOR_QUICKSTART.md](AI_PROCESSOR_QUICKSTART.md)
- 📝 История изменений: [CHANGELOG.md](CHANGELOG.md)

Удачи в использовании AI Processor! 🚀

