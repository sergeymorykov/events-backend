# Миграция на универсальный OpenAI-совместимый API

Данное руководство поможет вам мигрировать с версии 3.0.0 на 3.1.0 с новой системой интеграции с LLM.

## 📋 Что изменилось?

### Старая версия (3.0.0)

```bash
# Выбор провайдера
LLM_PROVIDER=gigachat  # или openai

# Креденшалы для конкретного провайдера
GIGACHAT_TOKEN=your_token
OPENAI_API_KEY=your_key
```

### Новая версия (3.1.0)

```bash
# Универсальный подход - один интерфейс для всех провайдеров
LLM_BASE_URL=https://api.mapleai.de/v1
LLM_API_KEYS=key1,key2,key3  # Поддержка ротации!
LLM_MODEL_NAME=gpt-4o
```

## 🔄 Пошаговая миграция

### Шаг 1: Обновите зависимости

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `openai>=1.52.0`
- `tenacity>=9.0.0`

### Шаг 2: Обновите `.env` файл

#### Вариант A: ZenMux (рекомендуется)

```bash
# Удалите старые переменные
# LLM_PROVIDER=...
# GIGACHAT_TOKEN=...
# OPENAI_API_KEY=...

# Добавьте новые
LLM_BASE_URL=https://api.mapleai.de/v1
LLM_API_KEYS=ваш_ключ_zenmux_1,ваш_ключ_zenmux_2
LLM_MODEL_NAME=gpt-4o
LLM_VISION_MODEL=gpt-4o
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000
```

#### Вариант B: OpenAI

```bash
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEYS=sk-your_openai_key
LLM_MODEL_NAME=gpt-4o
LLM_VISION_MODEL=gpt-4o
```

#### Вариант C: GigaChat

```bash
LLM_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1
LLM_API_KEYS=ваш_токен_gigachat
LLM_MODEL_NAME=GigaChat-Max
```

#### Вариант D: Обратная совместимость (не рекомендуется)

Если вы хотите временно сохранить старую конфигурацию:

```bash
# Оставьте старые переменные - они будут автоматически конвертированы
GIGACHAT_TOKEN=your_token
# или
OPENAI_API_KEY=your_key
```

**Примечание:** При использовании старых переменных:
- `LLM_BASE_URL` будет установлен по умолчанию
- Ротация ключей не будет доступна
- Рекомендуется мигрировать на новый формат

### Шаг 3: Обновите код (если используете программно)

#### Старый код

```python
from ai_processor import AIProcessor
from ai_processor.config import AIConfig

processor = AIProcessor(
    llm_provider=AIConfig.LLM_PROVIDER,
    gigachat_token=AIConfig.GIGACHAT_TOKEN,
    openai_api_key=AIConfig.OPENAI_API_KEY,
    # ...
)
```

#### Новый код

```python
from ai_processor import AIProcessor
from ai_processor.config import AIConfig

# Получение API ключей
api_keys = AIConfig.get_api_keys()

processor = AIProcessor(
    llm_base_url=AIConfig.LLM_BASE_URL,
    llm_api_keys=api_keys,
    llm_model_name=AIConfig.LLM_MODEL_NAME,
    llm_vision_model=AIConfig.LLM_VISION_MODEL,
    llm_temperature=AIConfig.LLM_TEMPERATURE,
    llm_max_tokens=AIConfig.LLM_MAX_TOKENS,
    # ...
)
```

### Шаг 4: Проверьте конфигурацию

```bash
python -c "from ai_processor.config import AIConfig; AIConfig.print_config()"
```

Вы должны увидеть:

```
============================================================
КОНФИГУРАЦИЯ AI ПРОЦЕССОРА:
  LLM Base URL: https://api.mapleai.de/v1
  LLM Model: gpt-4o
  Vision Model: gpt-4o
  Temperature: 0.7
  Max Tokens: 2000
  API Keys: 2 ключ(ей) настроено
  Kandinsky API: ✓ настроен
  Telegram API: ✓ настроен
  MongoDB URI: mongodb://localhost:27017/
  MongoDB DB: events_db
  Images Dir: images
============================================================
```

### Шаг 5: Тестирование

Протестируйте на одном посте:

```bash
python test_single_post.py --mock
```

Проверьте логи:

```bash
tail -f ai_processor.log
```

Убедитесь, что:
- ✅ Нет ошибок подключения к API
- ✅ Ротация ключей работает (если настроено несколько)
- ✅ Модель отвечает корректно
- ✅ Vision модель обрабатывает изображения

## 🚀 Новые возможности

### 1. Ротация API ключей

Теперь вы можете указать несколько ключей:

```bash
LLM_API_KEYS=key1,key2,key3
```

При достижении rate limit (429) модуль автоматически переключится на следующий ключ.

### 2. Retry механизм

Все запросы к LLM теперь автоматически повторяются:
- Максимум 3 попытки
- Экспоненциальная задержка 1-10 секунд
- Умная обработка ошибок

### 3. Гибкие параметры генерации

```bash
# Более детерминированный вывод
LLM_TEMPERATURE=0.3

# Более короткие ответы
LLM_MAX_TOKENS=1000

# Отдельная vision модель
LLM_VISION_MODEL=gpt-4o-vision
```

## 🔍 Проверка работы

### Тест 1: Базовая обработка

```bash
python test_single_post.py
```

Ожидаемый результат:
- Пост успешно обработан
- Извлечены все данные
- Нет ошибок в логах

### Тест 2: Ротация ключей (если настроено)

1. Установите очень низкий rate limit на первом ключе
2. Запустите обработку
3. Проверьте логи - должно быть переключение на второй ключ:

```
WARN: Переключение на следующий API ключ (index=1)
INFO: Ключ переключён после ошибки: 429 Rate limit exceeded
```

### Тест 3: Vision модель

Используйте пост с изображением:

```bash
python test_single_post.py
```

Проверьте, что `image_caption` был сгенерирован.

## 🐛 Troubleshooting

### Ошибка: "LLM_API_KEY, LLM_API_KEYS или хотя бы один из GIGACHAT_TOKEN/OPENAI_API_KEY должен быть указан"

**Решение:** Убедитесь, что хотя бы одна из переменных установлена:
- `LLM_API_KEY`
- `LLM_API_KEYS`
- `GIGACHAT_TOKEN`
- `OPENAI_API_KEY`

### Ошибка: "Connection error" или "Timeout"

**Возможные причины:**
1. Неверный `LLM_BASE_URL`
2. Нет интернета
3. API провайдер недоступен

**Решение:**
```bash
# Проверьте URL
curl $LLM_BASE_URL/models

# Проверьте API ключ
curl -H "Authorization: Bearer $LLM_API_KEY" $LLM_BASE_URL/models
```

### Ошибка: "401 Unauthorized"

**Решение:** Проверьте корректность API ключа:

```bash
echo $LLM_API_KEYS
```

### Ошибка: "429 Rate Limit"

Если ротация ключей не помогает:

1. Добавьте больше ключей
2. Увеличьте паузы между запросами
3. Используйте более дешевую модель

### Медленная обработка

**Оптимизация:**

```bash
# Уменьшите max_tokens
LLM_MAX_TOKENS=1000

# Используйте более быструю модель
LLM_MODEL_NAME=gpt-3.5-turbo  # для OpenAI
```

## 📚 Дополнительная документация

- `ai_processor/LLM_PROVIDERS.md` - примеры конфигурации для разных провайдеров
- `AI_PROCESSOR_QUICKSTART.md` - быстрый старт
- `ai_processor/README.md` - полная документация

## ❓ FAQ

### Можно ли использовать несколько провайдеров одновременно?

Нет, но вы можете создать несколько экземпляров `AIProcessor` с разными конфигурациями.

### Поддерживаются ли локальные модели?

Да! Используйте LocalAI или Ollama:

```bash
LLM_BASE_URL=http://localhost:11434/v1  # Ollama
LLM_API_KEYS=dummy_key
LLM_MODEL_NAME=llama3.1:8b
```

### Как добавить новый провайдер?

Если провайдер OpenAI-совместимый, просто укажите его `base_url` и API ключ. Никаких изменений в коде не требуется!

### Сколько ключей можно использовать для ротации?

Без ограничений. Рекомендуется 2-5 ключей для оптимального баланса.

### Как откатиться на старую версию?

```bash
git checkout v3.0.0
pip install -r requirements.txt
```

## 🎉 Заключение

После миграции вы получите:
- ✅ Поддержку любого OpenAI-совместимого провайдера
- ✅ Автоматическую ротацию ключей
- ✅ Надежный retry механизм
- ✅ Гибкую настройку генерации
- ✅ Улучшенную обработку ошибок

Если возникли вопросы, обратитесь к документации или создайте issue в репозитории.

