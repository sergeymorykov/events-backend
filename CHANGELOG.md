# Changelog

## [3.1.0] - 2025-11-23

### 🔄 Универсальная интеграция с LLM (Breaking Changes)

Переход на универсальный OpenAI-совместимый API вместо прямых HTTP запросов.

#### ✨ Новые возможности

- **Поддержка любого OpenAI-совместимого провайдера**:
  - ZenMux / MapleAI (рекомендуется)
  - OpenAI
  - GigaChat
  - LocalAI / Ollama
  - Groq, Together AI и другие

- **Автоматическая ротация API ключей**:
  - Поддержка нескольких ключей через `LLM_API_KEYS=key1,key2,key3`
  - Автоматическое переключение при rate limits (429)
  - Умное определение ошибок, требующих ротации

- **Retry механизм с экспоненциальной задержкой**:
  - Библиотека `tenacity` для надежных повторных попыток
  - Максимум 3 попытки на каждый ключ
  - Задержка 1-10 секунд между попытками

- **Гибкая настройка генерации**:
  - `LLM_TEMPERATURE` - температура генерации (0.0-2.0)
  - `LLM_MAX_TOKENS` - максимум токенов в ответе
  - `LLM_VISION_MODEL` - отдельная модель для обработки изображений

#### 🔧 Изменения в конфигурации

**Новые переменные окружения:**
```bash
LLM_BASE_URL=https://api.mapleai.de/v1  # Базовый URL API
LLM_API_KEYS=key1,key2,key3             # API ключи (с ротацией)
LLM_MODEL_NAME=gpt-4o                   # Название модели
LLM_VISION_MODEL=gpt-4o                 # Vision модель (опционально)
LLM_TEMPERATURE=0.7                     # Температура
LLM_MAX_TOKENS=2000                     # Максимум токенов
```

**Обратная совместимость:**
- Старые переменные `GIGACHAT_TOKEN` и `OPENAI_API_KEY` по-прежнему работают
- Автоматическая конвертация в новый формат
- Рекомендуется миграция на новые переменные

#### 📚 Новая документация

- `ai_processor/LLM_PROVIDERS.md` - примеры конфигурации для разных провайдеров
- Обновлены `AI_PROCESSOR_QUICKSTART.md` и `ai_processor/README.md`
- Примеры для ZenMux, OpenAI, GigaChat, LocalAI и других

#### 📦 Новые зависимости

- `openai>=1.52.0` - официальная библиотека OpenAI
- `tenacity>=9.0.0` - retry механизм

#### 🔄 Изменения в коде

- `LLMHandler` переписан с использованием `AsyncOpenAI`
- Удалены прямые HTTP запросы через `aiohttp` для LLM
- Универсальная нормализация сообщений
- Улучшенная обработка ошибок

## [3.0.0] - 2025-11-23

### 🤖 Новый модуль: AI Processor

Добавлен полноценный модуль для обработки сырых постов через AI с извлечением структурированных данных о событиях.

#### ✨ Основные возможности

- **Интеграция с LLM**:
  - Поддержка GigaChat API
  - Поддержка OpenAI API
  - Универсальный интерфейс через ENV-флаг `LLM_PROVIDER`
  - Извлечение структурированных данных (название, описание, дата, цена, категории, интересы)

- **Обработка изображений**:
  - Скачивание фото из Telegram через Telethon
  - Генерация изображений через Kandinsky 3.1 API (если фото отсутствует)
  - Конвертация в base64 для отправки в LLM
  - Сохранение в локальной папке `images/`

- **Vision-модели**:
  - Генерация текстового описания изображений
  - Отправка изображений в base64 формате
  - Использование описания для улучшения качества извлечения данных

- **Умное управление категориями и интересами**:
  - Автоматическое создание коллекций `categories` и `user_interests`
  - Передача существующих категорий в LLM для избежания дублирования
  - Счетчик использования для каждой категории/интереса
  - Индексы для быстрого поиска

- **Сохранение результатов**:
  - Коллекция `processed_events` с полной информацией
  - Индексы по `raw_post_id`, `date`, `categories`, `user_interests`
  - Связь с исходным постом через `raw_post_id`

- **Обработка ошибок**:
  - Устойчивость к таймаутам LLM
  - Обработка невалидного JSON
  - Частичные результаты при ошибках
  - Подробное логирование

#### 🏗️ Структура модуля

```
ai_processor/
├── __init__.py           # Инициализация и экспорты
├── config.py             # Конфигурация из ENV
├── models.py             # Pydantic модели для валидации
├── processor.py          # Основной класс AIProcessor
├── image_handler.py      # Работа с изображениями (скачивание, генерация)
├── llm_handler.py        # Интеграция с LLM (GigaChat, OpenAI)
├── db_handler.py         # Работа с MongoDB (категории, интересы, события)
└── README.md             # Полная документация
```

#### 📊 Новые коллекции MongoDB

1. **processed_events** - обработанные события:
   - Структурированные данные о событии
   - Ссылка на изображение
   - Описание изображения от AI
   - Связь с исходным постом

2. **categories** - уникальные категории:
   - `name` (unique) - название категории
   - `usage_count` - количество использований
   - `created_at` - дата создания

3. **user_interests** - интересы пользователей:
   - `name` (unique) - название интереса
   - `usage_count` - количество использований
   - `created_at` - дата создания

#### 🔧 Новые переменные окружения

```env
# AI Processor
LLM_PROVIDER=gigachat                    # gigachat или openai
GIGACHAT_TOKEN=your_token               # Токен GigaChat
OPENAI_API_KEY=your_key                 # API ключ OpenAI
OPENAI_BASE_URL=https://api.openai.com/v1

# Kandinsky
KANDINSKY_API_KEY=your_key              # API ключ Kandinsky
KANDINSKY_SECRET_KEY=your_secret        # Secret ключ Kandinsky

# Настройки
IMAGES_DIR=images                        # Папка для изображений
```

#### 🚀 Новые скрипты

- **run_ai_processor.py** - обработка всех необработанных постов
- **test_single_post.py** - тестирование на одном посте
  - `python test_single_post.py` - обработка реального поста из БД
  - `python test_single_post.py --mock` - тестирование с mock данными

#### 📚 Документация

- **AI_PROCESSOR_QUICKSTART.md** - быстрый старт AI Processor
- **ai_processor/README.md** - полная документация модуля
- Обновлен основной **README.md** с информацией о новом модуле

#### 📦 Новые зависимости

```
pydantic==2.9.2          # Валидация данных
aiohttp==3.10.10         # Асинхронные HTTP запросы
```

#### 🎯 Рабочий процесс

1. Парсинг постов из Telegram → `raw_posts`
2. AI обработка → `processed_events`, `categories`, `user_interests`
3. Готовые структурированные данные для приложения

#### 🔗 API интеграции

- **GigaChat** - https://developers.sber.ru/portal/products/gigachat
- **OpenAI** - https://platform.openai.com/api-keys
- **Kandinsky 3.1** - https://fusionbrain.ai/

---

## [2.3.0] - 2025-11-23

### ✨ Новые возможности

- **Поддержка приватных каналов**:
  - Парсинг каналов по invite hash (+hash формат)
  - Автоматическое присоединение к приватным каналам
  - Поддержка всех форматов: username, @username, t.me/..., +hash, t.me/+hash
  - Улучшенная обработка ошибок для приватных каналов

#### Использование приватных каналов

```env
# Формат 1: Только hash
CHANNEL_USERNAME=+lyO6V_0sqeA3OWRi

# Формат 2: Полная ссылка
CHANNEL_USERNAME=t.me/+lyO6V_0sqeA3OWRi

# Формат 3: Миксуем с публичными
CHANNEL_USERNAME=durov,+lyO6V_0sqeA3OWRi,telegram
```

Парсер автоматически:
- Определяет тип канала (публичный/приватный)
- Пытается присоединиться к приватному каналу если нужно
- Использует корректные методы для каждого типа

См. подробную документацию: **[PRIVATE_CHANNELS.md](PRIVATE_CHANNELS.md)**

#### Изменения в коде

- `config.py`:
  - Улучшен метод `get_channels()` — поддержка всех форматов ссылок
  - Автоматическая обработка t.me/ ссылок
  - Сохранение + для приватных каналов

- `parser.py`:
  - Добавлен импорт `ImportChatInviteRequest` для присоединения
  - Логика определения типа канала (публичный/приватный)
  - Автоматическое присоединение к приватным каналам
  - Улучшенное логирование с названием и ID канала
  - Корректная идентификация канала в БД

#### Документация

- Создан `PRIVATE_CHANNELS.md` — полное руководство по приватным каналам
- Обновлены примеры в `env.example`
- Обновлен `README.md` с информацией о приватных каналах

---

## [2.2.0] - 2025-11-23

### ✨ Новые возможности

- **Автоматический планировщик (Scheduler)**:
  - Запуск парсинга по расписанию (каждые 24 часа)
  - Два режима работы: интервальный и по времени (cron-like)
  - Автоматическая проверка новых постов без дубликатов
  - Отдельный лог-файл `telegram_scheduler.log`
  - Graceful shutdown (корректная остановка по Ctrl+C)

- **Парсинг по периоду вместо лимита постов**:
  - Заменен `POSTS_LIMIT` на `MONTHS_BACK`
  - Парсинг всех постов за последние N месяцев
  - Фильтрация по дате публикации сообщения
  - Более предсказуемое поведение

#### Использование планировщика

**Запуск:**
```bash
python run_scheduler.py
```

**Режимы работы** (в `telegram_parser/scheduler.py`):
```python
# Каждые 24 часа (по умолчанию)
scheduler.start(immediate=True, interval_hours=24)

# Ежедневно в 9:00
scheduler.start_daily(hour=9, minute=0, immediate=False)

# Каждые 6 часов
scheduler.start(immediate=True, interval_hours=6)
```

#### Изменения в конфигурации

**Удалено:**
- `POSTS_LIMIT` — лимит количества постов

**Добавлено:**
- `MONTHS_BACK=3` — период парсинга (в месяцах)

#### Изменения в коде

- `config.py`:
  - Заменено `POSTS_LIMIT` → `MONTHS_BACK`
  
- `parser.py`:
  - Изменена логика `parse_channel()` — использует `offset_date` вместо `limit`
  - Парсинг всех постов за период, а не фиксированное количество
  
- Новый модуль `scheduler.py`:
  - Класс `TelegramScheduler` с методами `start()` и `start_daily()`
  - Использует APScheduler для управления расписанием
  - Асинхронная работа с корректной обработкой сигналов

- Новый файл `run_scheduler.py` — точка входа для планировщика

#### Зависимости

- Добавлено: `apscheduler==3.10.4`

#### Документация

- Создан `SCHEDULER_GUIDE.md` — полное руководство по планировщику
- Обновлены `README.md` и `QUICKSTART.md` с информацией о планировщике
- Примеры использования разных режимов расписания
- Инструкции по запуску в фоне (systemd, Docker, Task Scheduler)

---

## [2.1.0] - 2025-11-23

### ✨ Новые возможности

- **Индивидуальные фильтры для каждого канала**:
  - Каждый канал теперь может иметь свои whitelist и blacklist хештегов
  - Формат: `CHANNEL_{channel}_WHITELIST` и `CHANNEL_{channel}_BLACKLIST`
  - Специфичные фильтры переопределяют глобальные для конкретного канала
  - Поддержка fallback на глобальные фильтры

#### Примеры использования

**Индивидуальные фильтры**:
```env
CHANNEL_USERNAME=moscowevents,spbevents

CHANNEL_moscowevents_WHITELIST=концерт,фестиваль
CHANNEL_moscowevents_BLACKLIST=18+

CHANNEL_spbevents_WHITELIST=выставка,театр
CHANNEL_spbevents_BLACKLIST=реклама
```

**Комбинированный подход**:
```env
CHANNEL_USERNAME=channel1,channel2,channel3

# Глобальные (для всех по умолчанию)
HASHTAG_WHITELIST=событие,мероприятие
HASHTAG_BLACKLIST=спам

# Переопределяем только для channel1
CHANNEL_channel1_WHITELIST=концерт,фестиваль
```

#### Изменения в коде

- `config.py`:
  - Новый метод `get_channel_filters(channel)` — получение фильтров для конкретного канала
  - Методы `get_whitelist_hashtags()` и `get_blacklist_hashtags()` теперь принимают параметр `channel`
  - Добавлен вспомогательный метод `_parse_hashtag_list()`

- `parser.py`:
  - Удален глобальный `hashtag_filter`
  - Добавлен словарь `channel_filters` для хранения фильтров каждого канала
  - Новый метод `_get_channel_filter(channel)` — ленивая инициализация фильтров
  - Улучшен вывод конфигурации при запуске (показывает специфичные фильтры)

- `check_config.py`:
  - Обновлен вывод фильтров (глобальные + специфичные для каждого канала)
  - Показывает, какие каналы используют глобальные, а какие — свои фильтры

#### Документация

- Обновлены `README.md`, `QUICKSTART.md`, `EXAMPLES.md`
- Добавлены примеры использования индивидуальных фильтров
- Обновлен `env.example` с подробными комментариями

---

## [2.0.0] - 2025-11-23

### 🎉 Полная переработка парсера

#### ✨ Новые возможности

- **Модульная архитектура** — весь код в папке `telegram_parser/`
- **Фильтрация по хештегам**:
  - Whitelist — только посты с указанными хештегами
  - Blacklist — исключение постов с запрещенными хештегами
  - Регистронезависимое извлечение хештегов
- **Интеллектуальный парсинг дат**:
  - Поддержка 10+ форматов дат (текстовые, числовые, относительные)
  - "23 ноября", "10.12.2025", "сегодня", "завтра"
  - Автоматическое добавление текущего года
- **Фильтрация по датам** — игнорирование прошедших мероприятий
- **Множественные каналы** — парсинг нескольких каналов одновременно
- **Расширенная статистика** — детальная информация о фильтрации
- **Причины фильтрации** — сохранение причины для каждого отфильтрованного поста

#### 🏗️ Архитектура

- `config.py` — централизованная конфигурация из .env
- `filters.py` — модуль фильтрации по хештегам
- `date_parser.py` — модуль парсинга дат
- `parser.py` — основной класс парсера
- `main.py` — точка входа в приложение

#### 📝 Структура данных MongoDB

Новые поля:
- `date_parsed` — распознанная дата мероприятия
- `hashtags` — массив хештегов (в нижнем регистре)
- `filtered_reason` — причина фильтрации (null если валиден)
- `message_date` — дата публикации поста

#### 🔧 Изменения в конфигурации

Переменные окружения (старые → новые):
- `TELEGRAM_API_ID` → `TG_API_ID`
- `TELEGRAM_API_HASH` → `TG_API_HASH`
- `TELEGRAM_CHANNEL_USERNAME` → `CHANNEL_USERNAME` (теперь поддерживает список)
- `TELEGRAM_SESSION_NAME` → `TG_SESSION_NAME`

Новые переменные:
- `HASHTAG_WHITELIST` — разрешенные хештеги (через запятую)
- `HASHTAG_BLACKLIST` — запрещенные хештеги (через запятую)

#### 📚 Документация

- Обновлен `README.md` с полным описанием
- Обновлен `QUICKSTART.md` с новыми инструкциями
- Добавлен `EXAMPLES.md` с примерами использования

#### 🚀 Запуск

Новый способ запуска:
```bash
python run_parser.py
```

Или напрямую:
```bash
python telegram_parser/main.py
```

#### 🔍 Проверка конфигурации

Обновлен скрипт проверки:
```bash
python telegram_parser/check_config.py
```

#### 📊 Статистика

Парсер теперь выводит подробную статистику:
- Всего постов обработано
- Сохранено валидных постов
- Отфильтровано по хештегам
- Отфильтровано по дате (прошлое)
- Без даты в тексте
- Дубликаты
- Ошибки

---

## [1.0.0] - 2025-11-20

### Первая версия

- Базовый парсинг Telegram-каналов
- Сохранение в MongoDB
- Извлечение текста, фото, метаданных
- Обработка rate limits
- Проверка дубликатов

