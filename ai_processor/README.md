# AI Processor

Модуль для обработки сырых постов из Telegram через AI (LLM и генерацию изображений).

## Возможности

- 🤖 **Универсальная интеграция с LLM**: поддержка любого OpenAI-совместимого API (ZenMux, OpenAI, GigaChat)
- 🔄 **Автоматическая ротация API ключей** при rate limits
- 🔁 **Retry механизм** с экспоненциальной задержкой
- 🖼️ **Обработка изображений**: скачивание из Telegram или генерация через Kandinsky 3.1
- 📝 **Генерация описаний изображений** через vision-модели
- 🗂️ **Умное управление категориями и интересами**: избегание дублирования
- 💾 **Сохранение обработанных событий** в MongoDB
- ⚡ **Асинхронная обработка** для высокой производительности

## Структура

```
ai_processor/
├── __init__.py           # Инициализация модуля
├── config.py             # Конфигурация из ENV
├── models.py             # Pydantic модели для валидации
├── processor.py          # Основной класс AIProcessor
├── image_handler.py      # Работа с изображениями
├── llm_handler.py        # Интеграция с LLM
├── db_handler.py         # Работа с MongoDB
└── README.md             # Документация
```

## Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Настройте переменные окружения в `.env`:

```bash
# ===== LLM API (универсальный OpenAI-совместимый подход) =====
LLM_BASE_URL=https://api.mapleai.de/v1  # ZenMux, OpenAI или другой
LLM_API_KEYS=key1,key2,key3  # Поддержка ротации!
LLM_MODEL_NAME=gpt-4o
LLM_VISION_MODEL=gpt-4o  # Опционально
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000

# Kandinsky для генерации изображений
KANDINSKY_API_KEY=your_key_here
KANDINSKY_SECRET_KEY=your_secret_here

# MongoDB (те же, что для парсера)
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=events_db

# Telegram API (для скачивания фото)
TG_API_ID=your_api_id
TG_API_HASH=your_api_hash
```

## Использование

### Через скрипт

Запустите обработку всех необработанных постов:

```bash
python run_ai_processor.py
```

### Программно

```python
import asyncio
from ai_processor import AIProcessor
from ai_processor.config import AIConfig

async def main():
    # Получение API ключей
    api_keys = AIConfig.get_api_keys()
    
    # Инициализация процессора
    processor = AIProcessor(
        llm_base_url=AIConfig.LLM_BASE_URL,
        llm_api_keys=api_keys,
        llm_model_name=AIConfig.LLM_MODEL_NAME,
        llm_vision_model=AIConfig.LLM_VISION_MODEL,
        llm_temperature=AIConfig.LLM_TEMPERATURE,
        llm_max_tokens=AIConfig.LLM_MAX_TOKENS,
        kandinsky_api_key=AIConfig.KANDINSKY_API_KEY,
        kandinsky_secret_key=AIConfig.KANDINSKY_SECRET_KEY,
        mongodb_uri=AIConfig.MONGODB_URI,
        mongodb_db_name=AIConfig.MONGODB_DB_NAME
    )
    
    # Обработка всех необработанных постов
    stats = await processor.process_all_unprocessed_posts()
    
    print(f"Обработано: {stats['success']}/{stats['total']}")
    
    processor.close()

asyncio.run(main())
```

### Обработка одного поста

```python
from ai_processor import AIProcessor

async def process_single_post():
    processor = AIProcessor(...)
    
    # Получаем сырой пост из БД
    raw_post = processor.db_handler.get_raw_post_by_id(post_id=123)
    
    # Обрабатываем
    result = await processor.process_raw_post(raw_post)
    
    if result:
        print(f"Событие: {result.title}")
        print(f"Дата: {result.date}")
        print(f"Категории: {result.categories}")
    
    processor.close()
```

## Схема данных

### Входные данные (raw_post)

```json
{
  "post_id": 12345,
  "text": "Текст поста с описанием события",
  "photo_url": {"photo_id": "...", "access_hash": "..."},
  "post_url": "https://t.me/channel/12345",
  "hashtags": ["событие", "концерт"]
}
```

### Выходные данные (ProcessedEvent)

```json
{
  "title": "Название события",
  "description": "Описание события",
  "date": "2025-11-23T19:00:00",
  "price": {
    "amount": 1500,
    "currency": "RUB"
  },
  "categories": ["музыка", "концерт"],
  "user_interests": ["живая музыка", "развлечения"],
  "image_url": "images/generated_20251123_120000.png",
  "image_caption": "Описание изображения от AI",
  "source_post_url": "https://t.me/channel/12345",
  "raw_post_id": 12345,
  "processed_at": "2025-11-23T12:00:00"
}
```

## Коллекции MongoDB

### processed_events

Обработанные события с полной информацией.

**Индексы:**
- `raw_post_id` (unique) - связь с исходным постом
- `processed_at` - время обработки
- `date` - дата события
- `categories` - категории
- `user_interests` - интересы

### categories

Уникальные категории событий.

**Поля:**
- `name` (unique) - название категории
- `usage_count` - количество использований
- `created_at` - дата создания

**Пример:**
```json
{
  "name": "концерт",
  "usage_count": 42,
  "created_at": "2025-11-23T10:00:00"
}
```

### user_interests

Уникальные интересы пользователей.

**Поля:**
- `name` (unique) - название интереса
- `usage_count` - количество использований
- `created_at` - дата создания

**Пример:**
```json
{
  "name": "живая музыка",
  "usage_count": 38,
  "created_at": "2025-11-23T10:00:00"
}
```

## Работа модуля

### Алгоритм обработки

1. **Получение поста** из коллекции `raw_posts`

2. **Обработка изображения:**
   - Если есть `photo_url` → скачивание через Telegram API
   - Если нет → генерация через Kandinsky 3.1

3. **Генерация описания изображения** (если фото было в посте):
   - Отправка в LLM в формате base64
   - Получение текстового описания

4. **Извлечение данных события:**
   - Получение существующих категорий и интересов из БД
   - Формирование промпта с контекстом (текст + изображение + хештеги)
   - Отправка в LLM
   - Парсинг JSON-ответа

5. **Сохранение:**
   - Валидация через Pydantic
   - Сохранение в `processed_events`
   - Обновление счетчиков в `categories` и `user_interests`

### Обработка ошибок

Модуль устойчив к ошибкам:

- **Таймауты LLM** → возврат частичного результата
- **Ошибки генерации изображений** → продолжение без изображения
- **Невалидный JSON от LLM** → логирование и частичный результат
- **Проблемы с БД** → логирование и продолжение работы

## Настройка LLM

### GigaChat

```bash
LLM_PROVIDER=gigachat
GIGACHAT_TOKEN=your_token_here
```

Получите токен: https://developers.sber.ru/portal/products/gigachat

### OpenAI

```bash
LLM_PROVIDER=openai
OPENAI_API_KEY=your_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
```

Получите ключ: https://platform.openai.com/api-keys

## Kandinsky 3.1

Для генерации изображений:

```bash
KANDINSKY_API_KEY=your_key_here
KANDINSKY_SECRET_KEY=your_secret_here
```

Получите ключи: https://fusionbrain.ai/

## Логирование

Логи сохраняются в файл `ai_processor.log`:

```
2025-11-23 12:00:00 | INFO     | ai_processor | Обработка поста ID: 12345
2025-11-23 12:00:05 | INFO     | ai_processor | Генерация изображения через Kandinsky...
2025-11-23 12:00:20 | INFO     | ai_processor | Изображение сгенерировано: images/generated_20251123_120020.png
2025-11-23 12:00:25 | INFO     | ai_processor | Генерация данных события через LLM...
2025-11-23 12:00:30 | INFO     | ai_processor | ✅ Пост обработан успешно
```

## Статистика

После обработки выводится статистика:

```
ИТОГИ ОБРАБОТКИ:
  Всего постов: 50
  Успешно обработано: 48
  Ошибок: 2

СТАТИСТИКА БД:
  Сырых постов: 100
  Обработанных событий: 48
  Уникальных категорий: 15
  Уникальных интересов: 23

  Топ категорий:
    - концерт: 12
    - выставка: 8
    - фестиваль: 7
    - спорт: 6
    - театр: 5

  Топ интересов:
    - музыка: 15
    - искусство: 10
    - культура: 8
    - спорт: 7
    - развлечения: 6
```

## Troubleshooting

### Ошибка: "Не удалось получить данные от LLM"

Проверьте:
- Корректность токена/API ключа
- Доступность API (интернет-соединение)
- Логи для деталей ошибки

### Ошибка: "Kandinsky API ключи не настроены"

Необязательно, но рекомендуется:
- Настройте `KANDINSKY_API_KEY` и `KANDINSKY_SECRET_KEY`
- Или используйте только посты с фото

### Ошибка: "Telegram клиент не инициализирован"

Для скачивания фото из Telegram:
- Настройте `TG_API_ID` и `TG_API_HASH`
- Авторизуйтесь через `python run_parser.py`

## Примеры

См. также:
- `run_ai_processor.py` - основной скрипт запуска
- `telegram_parser/` - парсер для получения сырых постов

## Лицензия

MIT

