# 🚀 Быстрый старт

## 1️⃣ Подготовка

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Создание конфигурации
```bash
cp env.example .env
```

## 2️⃣ Настройка .env

Отредактируйте файл `.env`:

```env
# 1. Получите API credentials на https://my.telegram.org/apps
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef

# 2. Укажите username канала (без @)
TELEGRAM_CHANNEL_USERNAME=durov

# 3. Количество постов для парсинга
POSTS_LIMIT=100

# 4. MongoDB (убедитесь что запущен!)
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=events_db
```

## 3️⃣ Запуск MongoDB

### Windows (с установленным MongoDB):
```bash
mongod
```

### Docker (если нет локального MongoDB):
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

## 4️⃣ Проверка конфигурации

```bash
python check_config.py
```

Должны увидеть:
```
✅ Все проверки пройдены успешно!
🎯 Можно запускать парсер: python telegram_parser.py
```

## 5️⃣ Запуск парсера

```bash
python telegram_parser.py
```

### При первом запуске:
1. Введите номер телефона: `+79991234567`
2. Введите код из Telegram
3. Если 2FA — введите пароль

Сессия сохранится — повторная авторизация не нужна!

## 6️⃣ Проверка результатов

### MongoDB CLI:
```bash
mongosh
use events_db
db.raw_posts.count()
db.raw_posts.findOne()
```

### Python:
```python
from pymongo import MongoClient

client = MongoClient('mongodb://localhost:27017/')
db = client['events_db']

# Количество постов
print(db.raw_posts.count_documents({}))

# Первый пост
print(db.raw_posts.find_one())
```

## 🎉 Готово!

Парсер работает! Проверьте логи в файле `telegram_parser.log`.

---

## 📚 Дополнительные возможности

### Скачивание фото
```bash
# Все фото
python download_photos.py

# Только 10 фото
python download_photos.py --limit 10
```

### Повторный запуск
Парсер не создаст дубликаты — проверяет посты по `post_id`.

---

## ❓ Частые проблемы

### Ошибка: "ServerSelectionTimeoutError"
**Решение:** Запустите MongoDB

### Ошибка: "FloodWaitError"
**Решение:** Парсер автоматически подождет и продолжит

### Ошибка: "ChannelPrivateError"
**Решение:** Убедитесь, что канал публичный

### Нет файла .env
**Решение:** `cp env.example .env` и заполните

---

## 📖 Полная документация

Смотрите [README.md](README.md) для подробной информации.

