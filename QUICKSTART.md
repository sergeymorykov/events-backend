# 🚀 Быстрый старт — Telegram Parser v2.0

## За 5 минут до первого запуска

### 1️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2️⃣ Создание конфигурации

```bash
cp env.example .env
```

### 3️⃣ Заполнение `.env`

Отредактируйте файл `.env`:

```env
# 1. Получите API credentials на https://my.telegram.org/apps
TG_API_ID=12345678
TG_API_HASH=0123456789abcdef0123456789abcdef

# 2. Укажите каналы для парсинга (через запятую, без @)
CHANNEL_USERNAME=durov,telegram

# 3. (Опционально) Глобальные фильтры по хештегам
# Применяются ко всем каналам, если не заданы специфичные
HASHTAG_WHITELIST=событие,мероприятие
HASHTAG_BLACKLIST=спам,реклама

# 4. (Опционально) Специфичные фильтры для отдельных каналов
# Переопределяют глобальные фильтры
CHANNEL_durov_WHITELIST=фестиваль,концерт,выставка
CHANNEL_durov_BLACKLIST=18+,закрытое

CHANNEL_telegram_WHITELIST=новости,обновления
CHANNEL_telegram_BLACKLIST=партнерство

# 5. MongoDB (по умолчанию локальный)
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=events_db

# 6. Настройки
MONTHS_BACK=3  # Парсинг постов за последние 3 месяца
TG_SESSION_NAME=telegram_parser_session
```

### 4️⃣ Запуск MongoDB

#### Если установлен локально (Windows):
```bash
mongod
```

#### Если нет — используйте Docker:
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### 5️⃣ Проверка конфигурации

```bash
python telegram_parser/check_config.py
```

✅ Если все зелёное — идём дальше!

### 6️⃣ Выбор режима работы

#### Вариант А: Разовый запуск (для тестирования)

```bash
python run_parser.py
```

Парсит все каналы один раз и завершается.

#### Вариант Б: Автоматический режим (рекомендуется)

```bash
python run_scheduler.py
```

**Планировщик будет:**
- ⏰ Запускать парсинг каждые 24 часа
- 🔄 Первый запуск — сразу при старте
- 🤖 Автоматически проверять новые посты
- 💾 Не дублировать уже сохраненные

**Логи**: `telegram_scheduler.log`  
**Остановка**: Ctrl+C

#### При первом запуске (любой вариант):

1. **Введите номер телефона**: `+79991234567`
2. **Введите код** из Telegram (придет в Saved Messages)
3. **Если 2FA** — введите пароль

**Сессия сохраняется** — повторная авторизация не нужна!

---

## 📊 Проверка результатов

### Через MongoDB Shell:

```bash
mongosh
use events_db
db.raw_posts.count()
db.raw_posts.findOne()
```

### Через Python:

```python
from pymongo import MongoClient

client = MongoClient('mongodb://localhost:27017/')
db = client['events_db']

# Общее количество
print(f"Всего постов: {db.raw_posts.count_documents({})}")

# Валидные (не отфильтрованные)
valid = db.raw_posts.count_documents({'filtered_reason': None})
print(f"Валидных: {valid}")

# Последние 5 валидных постов
posts = db.raw_posts.find({'filtered_reason': None}).limit(5)
for post in posts:
    print(f"\n{post['channel']}: {post['text'][:100]}")
    print(f"Дата: {post['date_parsed']}")
    print(f"Хештеги: {', '.join(post['hashtags'])}")
```

---

## 🎯 Как это работает

### Логика фильтрации

1. **Извлечение хештегов** из текста (регистронезависимо)
2. **Проверка blacklist** — если есть запрещенные хештеги → отфильтровать
3. **Проверка whitelist** — если задан и хештегов нет → отфильтровать
4. **Парсинг даты** из текста (поддержка 10+ форматов)
5. **Проверка даты** — если в прошлом → отфильтровать
6. **Сохранение** в MongoDB

### Поддерживаемые форматы дат

- **Текстовые**: "23 ноября", "10 декабря 2025"
- **Числовые**: "10.12.2025", "2025-12-10"
- **Относительные**: "сегодня", "завтра", "послезавтра"
- **Сокращения**: "23 ноя", "10 дек 2025"

Примеры:
```
✅ "Концерт 23 ноября в 19:00"  → 23.11.2025
✅ "Выставка 10.12.2025"        → 10.12.2025
✅ "Фестиваль завтра"           → завтрашняя дата
❌ "Мероприятие было 1 января"  → дата в прошлом
```

---

## 💡 Примеры конфигураций

### Пример 1: Глобальные фильтры для всех каналов

```env
CHANNEL_USERNAME=moscowevents,spbevents
HASHTAG_WHITELIST=фестиваль,концерт,выставка
HASHTAG_BLACKLIST=реклама,спам
```

**Результат**: Оба канала используют одинаковые фильтры

### Пример 2: Индивидуальные фильтры для каждого канала

```env
CHANNEL_USERNAME=moscowevents,kulturaru

# Для moscowevents - только музыкальные события
CHANNEL_moscowevents_WHITELIST=концерт,фестиваль,джаз
CHANNEL_moscowevents_BLACKLIST=18+

# Для kulturaru - культурные мероприятия
CHANNEL_kulturaru_WHITELIST=выставка,театр,музей
CHANNEL_kulturaru_BLACKLIST=реклама
```

**Результат**: Каждый канал со своими фильтрами

### Пример 3: Комбинированный подход

```env
CHANNEL_USERNAME=channel1,channel2,channel3

# Глобальные (по умолчанию для всех)
HASHTAG_WHITELIST=событие,мероприятие
HASHTAG_BLACKLIST=спам

# Специфичные только для channel1
CHANNEL_channel1_WHITELIST=концерт,фестиваль
# channel1 использует глобальный blacklist

# channel2 и channel3 используют глобальные фильтры
```

**Результат**: Гибкая настройка с fallback на глобальные фильтры

---

## 🔍 Отладка

### Логи

Все операции логируются в:
- **Консоль** (stdout)
- **Файл** `telegram_parser.log`

### Статистика после запуска:

```
📊 СТАТИСТИКА ПАРСИНГА:
  Всего постов обработано: 300
  ✅ Сохранено валидных постов: 85
  ❌ Отфильтровано по хештегам: 60
  ❌ Отфильтровано по дате: 40
  ❌ Без даты в тексте: 105
  ⏭️  Дубликаты: 10
```

### Если посты не сохраняются:

1. **Проверьте фильтры** — возможно, слишком строгие
2. **Посмотрите логи** — там будет причина фильтрации
3. **Проверьте даты** — парсер ищет даты в тексте

---

## ❓ Частые проблемы

### 🔴 "ServerSelectionTimeoutError"
**Причина**: MongoDB не запущен  
**Решение**: Запустите MongoDB (`mongod` или Docker)

### 🔴 "FloodWaitError"
**Причина**: Превышен лимит запросов к Telegram  
**Решение**: Парсер автоматически подождёт и продолжит

### 🔴 "ChannelPrivateError"
**Причина**: Канал приватный или недоступен  
**Решение**: Проверьте username, канал должен быть публичным

### 🔴 "Нет файла .env"
**Причина**: Не создан файл конфигурации  
**Решение**: `cp env.example .env` и заполните

### 🟡 "Мало постов сохраняется"
**Причина**: Строгие фильтры или нет дат в постах  
**Решение**: 
- Убрите whitelist временно: `HASHTAG_WHITELIST=`
- Посмотрите логи: `cat telegram_parser.log | grep "отфильтрован"`

### 🟡 "Неправильно парсятся даты"
**Причина**: Нестандартный формат даты  
**Решение**: Откройте Issue с примером текста

---

## 🎉 Готово!

Теперь у вас работает умный парсер событий из Telegram!

### Что дальше?

- 📚 Изучите [README.md](README.md) для подробной документации
- 🔧 Настройте фильтры под свои нужды
- 📊 Интегрируйте с вашим бэкендом
- 🚀 Запускайте по расписанию (cron/scheduler)

### Запуск по расписанию (опционально)

#### Linux/Mac — crontab:
```bash
# Каждый день в 9:00
0 9 * * * cd /path/to/events-backend && python run_parser.py >> parser.log 2>&1
```

#### Windows — Task Scheduler:
Создайте задачу, запускающую `run_parser.py` по расписанию

---

## 📖 Полная документация

См. [README.md](README.md)

---

**Есть вопросы?** Откройте Issue в репозитории!
