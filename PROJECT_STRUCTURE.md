# 📁 Структура проекта Events Backend

Актуально на: **01.02.2026**  
Версия: **2.0.0** (после интеграции Event Extraction)

---

## 🌳 Дерево проекта

```
events-backend/
│
├── 📁 src/                              # Исходный код модулей
│   ├── 📁 event_extraction/             # ✨ НОВЫЙ: Модуль извлечения событий
│   │   ├── __init__.py                  # Экспорты модуля
│   │   ├── config.py                    # Конфигурация (без Kandinsky)
│   │   ├── models.py                    # Pydantic модели с гибкими расписаниями
│   │   ├── langgraph_agent.py           # LangGraph агент для извлечения
│   │   ├── post_processor.py            # Главный оркестратор с дедупликацией
│   │   ├── deduplicator.py              # Семантическая дедупликация через Qdrant
│   │   ├── image_handler.py             # Генерация афиш (только LLM API)
│   │   └── README.md                    # Документация модуля
│   │
│   └── __init__.py                      # Инициализация src
│
├── 📁 api/                              # FastAPI приложение
│   ├── __init__.py
│   ├── main.py                          # Главный файл API
│   ├── auth.py                          # JWT аутентификация
│   ├── database.py                      # MongoDB клиент
│   ├── models.py                        # API модели
│   ├── interest_service.py              # Сервис интересов
│   └── README.md
│
├── 📁 telegram_parser/                  # Парсер Telegram каналов
│   ├── __init__.py
│   ├── main.py                          # Точка входа
│   ├── parser.py                        # Логика парсинга
│   ├── config.py                        # Конфигурация парсера
│   ├── filters.py                       # Фильтры постов
│   ├── date_parser.py                   # Парсинг дат
│   ├── download_photos.py               # Скачивание фото
│   ├── scheduler.py                     # Планировщик задач
│   ├── check_config.py                  # Проверка конфигурации
│   └── 📄 Документация:
│       ├── README.md
│       ├── QUICKSTART.md
│       ├── EXAMPLES.md
│       ├── PRIVATE_CHANNELS.md
│       └── GET_CHANNEL_ID.md
│
├── 📁 tests/                            # Тесты
│   ├── __init__.py
│   └── 📁 event_extraction/             # ✨ НОВЫЕ: Тесты event extraction
│       ├── __init__.py
│       ├── test_models.py               # Тесты Pydantic моделей
│       ├── test_deduplicator.py         # Тесты дедупликации
│       ├── test_image_handler.py        # Тесты обработки изображений
│       ├── test_post_processor.py       # Тесты главного процессора
│       └── test_langgraph_agent.py      # Тесты LangGraph агента
│
├── 📁 .cursor/                          # Настройки Cursor IDE
│   └── 📁 rules/
│       ├── base.mdc                     # Базовые правила для AI
│       └── fast-api.mdc                 # Правила FastAPI
│
├── 📁 images/                           # Папка для изображений
│   └── [сгенерированные афиши]
│
├── 📁 downloaded_photos/                # Папка для скачанных фото из Telegram
│   └── [фото из каналов]
│
├── 📄 Скрипты запуска:
│   ├── run_event_extraction.py          # ✨ НОВЫЙ: Запуск обработки событий
│   ├── run_parser.py                    # Запуск парсера
│   ├── run_api.py                       # Запуск API сервера
│   ├── run_scheduler.py                 # Запуск планировщика
│   ├── run_ai_processor.py              # ⚠️  УСТАРЕЛ: Старый процессор
│   ├── run_ai_processor_scheduler.py    # ⚠️  УСТАРЕЛ: Планировщик старого процессора
│   ├── get_channel_id.py                # Утилита для получения ID канала
│   ├── test_single_post.py              # Тест обработки одного поста
│   └── test_post_without_image.py       # Тест генерации афиши
│
├── 📄 Документация:
│   ├── README.md                         # Главный README проекта
│   ├── CHANGELOG.md                      # История изменений проекта
│   ├── GUIDE_EVENT_EXTRACTION.md         # ✨ НОВЫЙ: Полное руководство Event Extraction
│   ├── MIGRATION_EVENT_EXTRACTION.md     # ✨ НОВЫЙ: Руководство по миграции
│   ├── IMPLEMENTATION_SUMMARY.md         # ✨ НОВЫЙ: Итоговый отчёт реализации
│   ├── CHANGELOG_EVENT_EXTRACTION.md     # ✨ НОВЫЙ: Changelog модуля
│   ├── PROJECT_STRUCTURE.md              # ✨ НОВЫЙ: Этот файл
│   ├── MIGRATION_TO_ZENMUX.md            # Миграция на ZenMux API
│   └── DOCKER.md                         # Docker инструкции
│
├── 📄 Конфигурация:
│   ├── .env                              # Переменные окружения (НЕ в git)
│   ├── .env-buf                          # Бэкап .env
│   ├── env.example                       # Пример конфигурации
│   ├── requirements.txt                  # Python зависимости
│   ├── pytest.ini                        # ✨ НОВЫЙ: Настройки pytest
│   ├── .gitignore                        # Игнорируемые файлы
│   ├── .dockerignore                     # Игнорируемые файлы для Docker
│   ├── Dockerfile                        # Docker образ
│   ├── docker-compose.yml                # Docker Compose для разработки
│   └── docker-compose.prod.yml           # Docker Compose для production
│
└── 📄 Лицензия:
    └── LICENSE                           # MIT License

```

---

## 📊 Статистика проекта

### Модули

| Модуль | Файлов | Строк кода | Тестов | Статус |
|--------|--------|-----------|--------|--------|
| `src/event_extraction/` | 8 | ~2000 | 29 | ✅ Active |
| `api/` | 7 | ~1500 | 0 | ✅ Active |
| `telegram_parser/` | 10 | ~3000 | 0 | ✅ Active |
| `ai_processor/` | - | - | - | ❌ Removed |

### Документация

| Тип | Файлов | Строк |
|-----|--------|-------|
| Руководства | 4 | ~3000 |
| READMEs | 4 | ~1500 |
| Changelogs | 2 | ~1000 |
| Примеры | 3 | ~500 |

### Тесты

| Модуль | Unit тесты | Integration тесты |
|--------|-----------|------------------|
| Event Extraction | 29 | 0 |
| API | 0 | 0 |
| Parser | 0 | 0 |

---

## 🔍 Описание модулей

### 1. Event Extraction (`src/event_extraction/`)

**Назначение:** Извлечение событий из Telegram постов с семантической дедупликацией

**Ключевые файлы:**
- `langgraph_agent.py` - LangGraph агент для многошагового извлечения
- `post_processor.py` - главный оркестратор обработки
- `deduplicator.py` - дедупликация через Qdrant
- `image_handler.py` - генерация афиш через LLM API

**Зависимости:**
- MongoDB (motor) - хранение событий
- Qdrant - векторная БД для дедупликации
- OpenAI API - LLM и эмбеддинги
- LangGraph - многошаговая обработка

**Команда запуска:**
```bash
python run_event_extraction.py
```

### 2. FastAPI Application (`api/`)

**Назначение:** REST API для фронтенда

**Эндпоинты:**
- `/api/events` - получение событий
- `/api/auth` - аутентификация
- `/api/interests` - управление интересами

**Команда запуска:**
```bash
python run_api.py
# или
uvicorn api.main:app --reload
```

### 3. Telegram Parser (`telegram_parser/`)

**Назначение:** Парсинг постов из Telegram каналов

**Возможности:**
- Парсинг публичных/приватных каналов
- Фильтрация по хештегам (whitelist/blacklist)
- Скачивание фото
- Scheduled парсинг

**Команда запуска:**
```bash
python run_parser.py
```

---

## 🗄️ База данных MongoDB

### Коллекции

#### `raw_posts`
Сырые посты из Telegram
```javascript
{
  _id: ObjectId,
  post_id: Number,
  channel: String,
  text: String,
  photo_urls: Array<String>,
  hashtags: Array<String>,
  message_date: Date,
  post_url: String,
  created_at: Date
}
```

#### `events`
Обработанные события
```javascript
{
  _id: ObjectId,
  title: String,
  description: String,
  schedule: {
    type: "exact|recurring_weekly|fuzzy",
    date_start: Date,
    schedule: Object,
    ...
  },
  location: String,
  address: String,
  price: {
    amount: Number,
    currency: String,
    is_free: Boolean
  },
  categories: Array<String>,
  user_interests: Array<String>,
  images: Array<String>,
  poster_generated: Boolean,
  sources: Array<{
    channel: String,
    post_id: Number,
    post_url: String
  }>,
  canonical_hash: String,
  processed_at: Date
}
```

#### `processed_posts`
Лог обработанных постов (для идемпотентности)
```javascript
{
  _id: ObjectId,
  post_id: Number,
  channel: String,
  processed_at: Date,
  event_ids: Array<String>,
  events_count: Number
}
```

#### `users`
Пользователи API
```javascript
{
  _id: ObjectId,
  email: String,
  hashed_password: String,
  interests: Array<String>,
  created_at: Date
}
```

---

## 🔍 Qdrant Collections

### `events`
Векторы событий для дедупликации

**Конфигурация:**
- Vector size: 1536 (OpenAI embeddings)
- Distance: Cosine
- Similarity threshold: 0.92

**Payload:**
```javascript
{
  event_id: String,
  canonical_hash: String,
  title: String,
  description: String,
  location: String,
  categories: Array<String>,
  sources: Array<Object>,
  schedule_type: String,
  processed_at: String
}
```

---

## 🚀 Команды запуска

### Development

```bash
# Event Extraction
python run_event_extraction.py          # Обработка всех новых постов
python run_event_extraction.py 50       # Обработка 50 постов

# Telegram Parser
python run_parser.py                     # Разовый парсинг
python run_scheduler.py                  # Scheduled парсинг

# FastAPI
python run_api.py                        # API сервер (порт 8000)
uvicorn api.main:app --reload            # С hot-reload

# Тестирование
pytest tests/event_extraction/ -v        # Все тесты
pytest --cov=src.event_extraction        # С покрытием
```

### Production (Docker)

```bash
# Сборка и запуск всех сервисов
docker-compose -f docker-compose.prod.yml up -d

# Только Qdrant
docker run -p 6333:6333 qdrant/qdrant

# Только MongoDB
docker run -p 27017:27017 mongo:latest
```

---

## 📦 Зависимости

### Python (requirements.txt)

**Core:**
- `telethon==1.36.0` - Telegram API
- `pymongo==4.6.1` - MongoDB driver
- `pydantic==2.9.2` - Валидация данных
- `python-dotenv==1.0.0` - Переменные окружения

**Event Extraction:**
- `langgraph==0.1.8` - LangGraph агент
- `qdrant-client==1.10.0` - Qdrant клиент
- `python-dateutil==2.9.0` - Парсинг дат
- `openai>=1.52.0` - LLM API
- `aiohttp==3.10.10` - HTTP клиент

**FastAPI:**
- `fastapi>=0.104.0` - Web framework
- `uvicorn[standard]>=0.24.0` - ASGI сервер
- `motor>=3.3.2` - Async MongoDB
- `python-jose[cryptography]>=3.3.0` - JWT

**Testing:**
- `pytest>=7.4.0` - Тестовый фреймворк
- `pytest-asyncio>=0.21.0` - Async тесты
- `pytest-cov>=4.1.0` - Покрытие

**Другие:**
- `apscheduler==3.10.4` - Планировщик задач
- `Pillow>=10.0.0` - Обработка изображений
- `tenacity>=9.0.0` - Retry механизм
- `bcrypt>=4.0.0` - Хэширование паролей

### External Services

- **MongoDB 6+** - основная БД
- **Qdrant 1.10+** - векторная БД
- **OpenAI API** - LLM и эмбеддинги
- **Telegram API** - парсинг каналов

---

## 🔐 Переменные окружения

См. `env.example` для полного списка.

### Критичные:
```env
# Telegram
TG_API_ID=...
TG_API_HASH=...

# MongoDB
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=events_db

# LLM
LLM_BASE_URL=https://api.mapleai.de/v1
LLM_MODEL_NAME=gpt-4o
LLM_API_KEYS=key1,key2

# Qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333
```

---

## 📈 Метрики производительности

### Event Extraction
- Обработка 100 постов: < 60 сек
- Извлечение события: 2-5 сек
- Генерация афиши: 5-10 сек
- Дедупликация: < 100 ms

### API
- Response time: < 100 ms (без LLM)
- Throughput: ~1000 req/sec
- Latency p95: < 200 ms

### Parser
- Парсинг канала (3 месяца): ~5-10 мин
- Скачивание фото: ~1-2 сек/фото

---

## 🔄 Workflow обработки

```
┌─────────────────┐
│ Telegram Parser │
│  (run_parser)   │
└────────┬────────┘
         │
         v
┌─────────────────┐
│   raw_posts     │ (MongoDB)
│   collection    │
└────────┬────────┘
         │
         v
┌─────────────────┐
│ Event Extraction│
│  (run_event_    │
│   extraction)   │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    v         v
┌────────┐ ┌────────┐
│ events │ │ Qdrant │
│  (DB)  │ │(vector)│
└────┬───┘ └────────┘
     │
     v
┌─────────────────┐
│   FastAPI       │
│   (run_api)     │
└─────────────────┘
     │
     v
┌─────────────────┐
│   Frontend      │
└─────────────────┘
```

---

## 🆕 Что изменилось в версии 2.0.0

### ✨ Добавлено
- ✅ Модуль `src/event_extraction/` (8 файлов)
- ✅ 29 unit тестов
- ✅ LangGraph агент для извлечения
- ✅ Qdrant дедупликация
- ✅ Гибкие расписания (3 типа)
- ✅ Множественные источники событий
- ✅ 4 новых документационных файла

### ❌ Удалено
- ❌ Папка `ai_processor/`
- ❌ Kandinsky API поддержка
- ❌ Google GenAI SDK клиент
- ❌ Старые модели данных

### 🔄 Изменено
- Генерация изображений: только LLM API
- Модель события: `ProcessedEvent` → `StructuredEvent`
- API процессора: новая структура инициализации

---

## 📚 Документация

### Руководства
- **README.md** - главная документация проекта
- **GUIDE_EVENT_EXTRACTION.md** - полное руководство по Event Extraction
- **MIGRATION_EVENT_EXTRACTION.md** - руководство по миграции
- **telegram_parser/QUICKSTART.md** - быстрый старт парсера

### Технические документы
- **IMPLEMENTATION_SUMMARY.md** - итоговый отчёт реализации
- **CHANGELOG_EVENT_EXTRACTION.md** - история изменений модуля
- **PROJECT_STRUCTURE.md** - этот файл

### API документация
- FastAPI: `http://localhost:8000/docs` (Swagger)
- FastAPI: `http://localhost:8000/redoc` (ReDoc)

---

## 🤝 Контрибьютинг

### Как добавить новый модуль:
1. Создайте папку в `src/`
2. Добавьте `__init__.py` с экспортами
3. Создайте `README.md` с описанием
4. Добавьте тесты в `tests/`
5. Обновите `PROJECT_STRUCTURE.md`

### Стиль кода:
- Русские комментарии
- Функциональный подход
- Pydantic для валидации
- Async/await для I/O
- Type hints везде

---

## 📞 Support

**Документация:** См. файлы в корне проекта  
**Тесты:** `pytest tests/ -v`  
**Issues:** GitHub Issues  
**Версия:** 2.0.0  
**Дата:** 01.02.2026

---

**Конец документа**
